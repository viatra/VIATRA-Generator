problem builtin.

abstract class node {
	node[] equals opposite equals
}

pred exists(node node).

abstract class domain extends node.

abstract class data extends node.

enum bool {
	true, false
}

class real extends data.

class int extends real.

class string extends data.
